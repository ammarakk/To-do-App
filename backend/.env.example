# ============================================================================
# Environment Variables Template for Todo Backend
# ============================================================================
#
# IMPORTANT SECURITY NOTICE:
# - DO NOT commit the actual .env file to version control
# - This file (.env.example) is safe to commit
# - Copy this file to .env and fill in real values for local development
# - Get Neon database URL from: https://neon.tech
# - Generate JWT secret with: openssl rand -hex 32
#
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
API_PORT=8000
API_HOST=0.0.0.0
ENVIRONMENT=development

# ----------------------------------------------------------------------------
# Database Configuration (Neon PostgreSQL)
# ----------------------------------------------------------------------------
# Where to find these values:
# 1. Go to https://neon.tech
# 2. Create a new project or select existing one
# 3. Copy the connection string from the dashboard
#
# DATABASE_URL
# Purpose: PostgreSQL connection string for Neon
# Format: postgresql://username:password@ep-xxx.aws.neon.tech/neondb?sslmode=require
# Example: postgresql://john:secret123@ep-cool-name.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=your_neon_database_url_here

# ----------------------------------------------------------------------------
# JWT Configuration
# ----------------------------------------------------------------------------
# Where to generate JWT secret:
# - Run: openssl rand -hex 32 (on Linux/Mac/Windows Git Bash)
# - Or use: https://www.uuidgenerator.net/api/guid (take first 32 chars)
#
# JWT_SECRET_KEY
# Purpose: Cryptographic key for signing JWT tokens
# Requirements: At least 32 characters, cryptographically random
# Security: NEVER expose in frontend, NEVER commit to git
# WARNING: This secret key must be kept secure - rotate if exposed
# Format: Hex string or random string (min 32 chars)
# Example: a1b2c3d4e5f6... (64 hex chars)
JWT_SECRET_KEY=your_jwt_secret_key_at_least_32_chars

# JWT_ALGORITHM
# Purpose: Algorithm for JWT token signing
# Default: HS256 (HMAC-SHA256)
# Options: HS256, HS384, HS512 (symmetric keys)
JWT_ALGORITHM=HS256

# ACCESS_TOKEN_EXPIRE_MINUTES
# Purpose: Access token lifetime in minutes
# Default: 15 minutes
# Recommendation: 5-30 minutes for security
ACCESS_TOKEN_EXPIRE_MINUTES=15

# REFRESH_TOKEN_EXPIRE_DAYS
# Purpose: Refresh token lifetime in days
# Default: 7 days
# Recommendation: 7-30 days depending on security requirements
REFRESH_TOKEN_EXPIRE_DAYS=7

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------
# List of allowed origins for Cross-Origin Resource Sharing
# Comma-separated list of frontend URLs
# Format: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# ----------------------------------------------------------------------------
# Security Guidelines
# ----------------------------------------------------------------------------
#
# KEY USAGE RULES:
# 1. DATABASE_URL
#    - Use in: Backend (FastAPI server) ONLY
#    - Contains: Username, password, host, database name
#    - NEVER expose in frontend
#    - NEVER commit to git
#    - Always use SSL (sslmode=require)
#
# 2. JWT_SECRET_KEY
#    - Use in: Backend (FastAPI server) ONLY
#    - Purpose: Sign and verify JWT tokens
#    - NEVER use in frontend
#    - NEVER commit to git
#    - Must be at least 32 characters
#    - Rotate if accidentally exposed
#
# TOKEN SECURITY:
# - Access tokens: Short-lived (15 min), stored in memory/cookies
# - Refresh tokens: Long-lived (7 days), stored securely in database
# - Always use HTTPS in production
# - Implement token rotation on refresh
#
# GIT SAFETY:
# - Add .env to .gitignore
# - Never commit real secrets
# - Use .env.example for template (this file)
#
# PRODUCTION DEPLOYMENT:
# - Set these variables in your hosting platform (Vercel, Railway, etc.)
# - Do not use .env files in production
# - Use platform-specific secret management
# - Enable SSL for database connections
#
# FOR MORE INFORMATION:
# - Neon Docs: https://neon.tech/docs
# - SQLAlchemy Async: https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html
# - FastAPI Security: https://fastapi.tiangolo.com/tutorial/security/
# - JWT Best Practices: https://tools.ietf.org/html/rfc8725
#
