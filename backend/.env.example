# ============================================================================
# Environment Variables Template for Todo Backend
# ============================================================================
#
# IMPORTANT SECURITY NOTICE:
# - DO NOT commit the actual .env file to version control
# - This file (.env.example) is safe to commit
# - Copy this file to .env and fill in real values for local development
# - Get your values from: https://app.supabase.com/project/_/settings/api
#
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
API_PORT=8000
API_HOST=0.0.0.0
ENVIRONMENT=development

# ----------------------------------------------------------------------------
# Supabase Configuration
# ----------------------------------------------------------------------------
# Where to find these values:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Navigate to Settings → API
# 4. Copy the values from "Project URL" and "API Keys" sections
#
# SUPABASE_URL
# Purpose: Base URL for your Supabase project
# Format: https://your-project-id.supabase.co
# Example: https://abcdefgh.supabase.co
SUPABASE_URL=your_supabase_url_here

# SUPABASE_ANON_KEY (Public/Anonymous Key)
# Purpose: Public key for client-side access (RLS enforced)
# Usage: Frontend only (browser/Next.js)
# Security: Safe to expose in client code
# Format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# SUPABASE_SERVICE_ROLE_KEY (Backend-Only Key)
# Purpose: Privileged key for backend operations (bypasses RLS)
# Usage: Backend server ONLY (FastAPI)
# Security: NEVER expose in frontend, NEVER commit to git
# WARNING: This key can bypass Row Level Security - use with extreme caution
# Format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------
# List of allowed origins for Cross-Origin Resource Sharing
# Comma-separated list of frontend URLs
# Format: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# ----------------------------------------------------------------------------
# Security Guidelines
# ----------------------------------------------------------------------------
#
# KEY USAGE RULES:
# 1. ANON_KEY (Public Key)
#    - Use in: Frontend (Next.js app)
#    - Capabilities: Access data with RLS policies enforced
#    - Safe to expose in browser
#
# 2. SERVICE_ROLE_KEY (Backend Key)
#    - Use in: Backend (FastAPI server) ONLY
#    - Capabilities: Bypasses RLS, full database access
#    - NEVER use in frontend
#    - NEVER commit to git
#    - Rotate if accidentally exposed
#
# GIT SAFETY:
# - Add .env to .gitignore
# - Never commit real keys
# - Use .env.example for template (this file)
#
# PRODUCTION DEPLOYMENT:
# - Set these variables in your hosting platform (Vercel, Railway, etc.)
# - Do not use .env files in production
# - Use platform-specific secret management
#
# FOR MORE INFORMATION:
# - Supabase API Docs: https://supabase.com/docs/guides/api
# - Security Best Practices: https://supabase.com/docs/guides/platform/security
# - JWT Verification: https://supabase.com/docs/guides/auth/server-side-rendering
#
